export const environment = {
  production: true,
  // TODO(produção): substituir pelo domínio REAL da API antes do build de release.
  // Enquanto estiver 'api.example.com', o app mobile NÃO funcionará em produção.
  apiBaseUrl: 'https://api.example.com/api/v1',
};
